# smart-approval 插件开发与使用规范指南 (AGENTS & DEVELOPER GUIDE)

> 本指南专门提供给用户与后续负责代码编写/配置维护的 Agent（如 Codex、Luna 等），请严格遵循以下架构设计与操作约束。

---

## 一、 顶层架构概念（双预设模型）

系统严格划分为 **「模式 (Mode) -> 空间预设 (Spaces) -> 权限预设 (Presets)」** 三层结构：

```text
┌─────────────────────────────────────────────────────────────┐
│ 模式 (Mode): 顶层全局运行方案 (如 delegate / full_allow)    │
└──────────────────────────────┬──────────────────────────────┘
                               │ 包含一组空间预设 + other
       ┌───────────────────────┴───────────────────────┐
       ▼                                               ▼
┌──────────────────────────────┐       ┌──────────────────────────────┐
│  空间预设 (Space): 路径划分  │       │  兜底空间 (other)            │
│  - paths: ["~/workspace/**"] │       │  - preset: "developer"       │
│  - preset: "developer"       │       │  (当路径未命中上方空间时生效)│
└──────────────┬───────────────┘       └──────────────┬───────────────┘
               │ 关联权限预设                         │
               ▼                                      ▼
┌─────────────────────────────────────────────────────────────┐
│  权限预设 (Preset): 纯权限规则库                            │
│  1. 白名单 (rules.allow): 命中直接放行 (0 Token, 0 弹窗)    │
│  2. 黑名单 (rules.deny):  命中直接阻断 (0 弹窗)             │
│  3. 灰名单策略 (undecided_strategy): 未知命令的具体处置方式  │
│  4. 工具权限 (tools.allow / tools.deny)                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、 灰名单（未定名单）策略的具体选项

当命令或工具调用**既不在明确白名单，也不在黑名单**时，定义为【灰名单 / 未定名单】。
权限预设（Preset）中的 `undecided_strategy` 必须且仅能配置为以下 **3 个选项之一**：

| 选项值 (`undecided_strategy`) | 显示名称 | 核心处置逻辑 | 推荐适用场景 |
| :--- | :--- | :--- | :--- |
| **`ai_with_ask`** | **AI 智能研判 + 拿不准问我** | 自动交由后台 AI 审查模型研判。AI 判定安全则**静默放行（0 弹窗）**；仅当 AI 判定高危或无法推测意图时，才**弹窗向主人确认**。 | **主开发工作区（默认推荐）** |
| **`ai_only`** | **纯 AI 全权打理** | 自动交由后台 AI 审查模型研判。AI 判定安全直接放行；AI 判定高危或拿不准时**直接拒绝阻断，绝不弹窗打扰人类**。 | **临时目录、构建缓存、自动化沙盒** |
| **`ask_human`** | **严格问我** | 凡是未在明确白名单内的命令，**不经 AI 预判，一律直接弹窗请主人亲自确认**。 | **系统根目录、配置目录、密钥凭证区** |

---

## 三、 TUI 交互与快捷指令规范

1. **`/am`**：专门用于挑选与切换用于智能安全审查的大语言模型（如 `cpa-vps/gemini-3.7-flash-high`）。
2. **`/ae`**：
   - **首页展示**：每一个模式仅占单一行（当前生效项带 `✓` 标记）；
   - **操作逻辑**：
     - **单次回车**：切换/激活所选模式；
     - **两下回车**（或在已激活项按回车）：进入该模式专属的**两大预设查看界面**：
       - **`1. 权限预设`**：查看该模式引用的黑白名单规则明细及**灰名单处理方式**；
       - **`2. 空间预设`**：查看该模式的路径映射规则以及 **`* other` 兜底空间**设置。
3. **`/ae <需求>` / `/an <需求>`**：开启一次 Agent 新增模式任务。`/an` 是无法传递 `/ae` 参数时的兼容入口。
4. **`/ad`**：进入用户模式删除界面。内置模式和当前模式不可删除；连续两次 `Ctrl+D` 才确认。

---

## 四、 Agent 编写与修改规范（严禁乱改代码）

后续 Agent 在协助用户用自然语言调整配置时，必须遵守以下铁律：

1. **统一只修改全局配置文件**：
   - 配置文件唯一物理路径：`~/.config/opencode/command-approval.jsonc`。
   - **严禁**创建额外的乱七八糟的临时配置文件。
2. **严禁破坏防更新隔离层**：
   - 外部加载器 `~/.config/opencode/plugins/smart-approval.ts` 必须保持 `try...catch` 隔离沙箱，绝不允许向 OpenCode 宿主抛出未捕获异常。
3. **空间与预设解耦约束**：
   - 路径配置属于**空间预设 (Spaces)**；
   - 指令的白名单、黑名单与灰名单策略属于**权限预设 (Presets)**；
   - 兜底空间统一写作 **`other`**。
4. **新增模式必须通过插件工具**：
   - Agent 只能调用 `approval_mode_create` 提交完整草案，严禁直接编辑配置文件；
   - 模式 ID 使用小写字母、数字、下划线，权限预设使用 `<mode_id>.<name>` 命名空间；
   - 每个模式至少包含一个空间，并必须提供 `other` 兜底；
   - 新模式不得覆盖已有模式或预设，创建后不得自动切换为生效模式；
   - 插件必须校验预设引用、规则表达式、V4 Schema 和配置版本，并使用原子替换保存。
5. **删除保护**：
   - `delegate`、`full_allow` 永久禁止删除；
   - 当前生效模式必须先切走；
   - 只删除目标模式独占、命名空间内且不再被任何模式引用的权限预设。
